package iskallia.vault.effect;

import net.minecraft.potion.EffectType;
import net.minecraft.util.ResourceLocation;

public class ImmunityEffect extends BasicEffect {
   public ImmunityEffect(EffectType typeIn, int liquidColorIn, ResourceLocation id) {
      super(typeIn, liquidColorIn, id);
   }
}
