package iskallia.vault.util.calc;

import net.minecraft.server.level.ServerPlayer;

public class TotemEffectRadiusHelper {
   private TotemEffectRadiusHelper() {
   }

   public static float adjustTotemEffectRadiusHelper(ServerPlayer player, float effectRadius) {
      return effectRadius;
   }
}
